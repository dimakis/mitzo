# OpenTelemetry — set to enable browser-side tracing
# Traces are exported to this endpoint (Jaeger OTLP HTTP receiver)
# Start Jaeger: npm run tracing:up (from repo root)
# VITE_OTEL_ENDPOINT=http://localhost:4318
