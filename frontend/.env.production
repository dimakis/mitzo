# This is a reviewed release input, not a development default. Vite bakes it
# into the browser/PWA bundle, and the production preflight verifies the exact
# value against infra/openshell/production-stack.lock.json.
VITE_API_BASE_URL=https://dimakis-mac.taildfe858.ts.net:3100
