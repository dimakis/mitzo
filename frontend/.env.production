# Production builds are same-origin by default.  Deployments pass their own
# reviewed MITZO_PUBLIC_ORIGIN to scripts/build-production-frontend.mjs, which
# bakes it as VITE_API_BASE_URL and is checked by the production preflight.
# Do not put a host-specific Tailnet URL in this tracked generic configuration.
