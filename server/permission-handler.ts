// Re-export from @mitzo/harness. The canonical implementation lives there.
// Server code continues importing from './permission-handler.js' unchanged.
export { buildPermissionHandler } from '@mitzo/harness';
