export const HTML_ARTIFACT_INSTRUCTIONS =
  '\n- For visual proposals and UI prototypes, create a self-contained .html file in the workspace and link the file path in your response so Mitzo can render it. Do not create Cursor or Canvas artifacts; Mitzo cannot render those.\n';
